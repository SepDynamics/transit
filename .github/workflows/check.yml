name: Check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  transit-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run reproducible transit checks
        run: make ci-check
