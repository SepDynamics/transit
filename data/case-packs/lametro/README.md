# LA Metro Case Packs

This directory intentionally contains no unapproved Swiftly captures. Populate
it only from agency-authorized archival snapshots and labels. The initial suite
must cover bus, rail, detour, alert/protobuf, and quiet-control windows, then
run through the same replay regression gate as MBTA.
